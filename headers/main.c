#include <stdlib.h>
#include <stdio.h>

#include "sum.h"


int sumOfTwoNumbers(int a, int b)
{
    return LIMIT + LIMIT;
}

int main()
{   
    int sum = sumOfTwoNumbers(1,2);
    printf("The sum of the 2 numbers is %d\n", sum);
    return 0;
}